import React from 'react';

// Styles
import './Login.scss';

const Login = () => (
  <div className="login">
    <div className="container">
      <h1>Login</h1>
    </div>
  </div>
);

export default Login;
